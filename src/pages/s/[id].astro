---
// The session reader now lives in the 3-pane shell. Keep old /s/<id> links
// working by redirecting into the shell with the session selected.
const { id } = Astro.params;
if (!id) return new Response("missing id", { status: 400 });
return Astro.redirect(`/?session=${id}`);
---

---
import type { MessageRow } from "../types";

interface Props {
  message: MessageRow;
}
const { message } = Astro.props;
const preview = (message.textContent ?? "").slice(0, 80).replace(/\s+/g, " ");
const charCount = (message.textContent ?? "").length;
---
<details class="border border-slate-200 rounded-lg bg-white">
  <summary class="px-3 py-2 cursor-pointer text-sm flex items-center gap-2 hover:bg-slate-50 min-w-0">
    <span class="text-emerald-700 flex-shrink-0">← tool result</span>
    <span class="text-slate-400 flex-shrink-0">·</span>
    <span class="text-slate-500 truncate min-w-0 flex-1">{preview}</span>
    <span class="text-slate-400 text-xs ml-auto whitespace-nowrap flex-shrink-0">{charCount} chars</span>
  </summary>
  <pre class="text-xs bg-slate-50 px-3 py-2 overflow-x-auto whitespace-pre-wrap break-words border-t border-slate-200">{message.textContent}</pre>
</details>
